# AI Flow 环境变量

# Claude API 配置（必需）
ANTHROPIC_BASE_URL=http://localhost:3009
ANTHROPIC_API_KEY=custom

# 工作目录配置（必需）
# 容器环境使用: PROJECT_ROOT=/app
PROJECT_ROOT=/app

# LLMs API 配置（必需）
OPENROUTER_API_KEY=sk-your_openrouter_api_key_here

# 注意：ORG_ID 和 FLOW_ID 需要在启动时通过 -e 参数动态传递