# AI Flow 环境变量配置

# 使用外部 llms（推荐）
ANTHROPIC_BASE_URL=http://external-llms:3009

# 使用内部 llms（可选）
# OPENROUTER_API_KEY=sk-your_openrouter_api_key_here

# 注意：ORG_ID 和 FLOW_ID 需要在启动时通过启动脚本传递

# MCP 服务器配置（双下划线式，层级清晰）
# 方式一：最小配置（只配置 URL，type 默认 http）
MCP_SERVERS__OP__URL=http://op-mcp:8008

# 方式二：完整配置（显式指定所有参数）
# MCP_SERVERS__OP__TYPE=http
# MCP_SERVERS__OP__URL=http://op-mcp:8008
# MCP_SERVERS__OP__HEADERS={"x-api-key":"your-key"}

# 配置多个 MCP 服务器
# MCP_SERVERS__CUSTOM__URL=http://custom-mcp:9000